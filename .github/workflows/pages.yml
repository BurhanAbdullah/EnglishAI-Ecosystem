name: Deploy EnglishAI website

on:
  push:
    branches: [main]
    paths:
      - 'englishai/**'
      - '.github/workflows/pages.yml'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: github-pages
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Checkout
        uses: actions/checkout@v6
      - name: Configure GitHub Pages
        uses: actions/configure-pages@v5
      - name: Build website artifact
        shell: bash
        run: |
          set -euo pipefail
          rm -rf _site
          mkdir -p _site
          cp englishai/*.html _site/
          cp englishai/*.svg _site/
          cp englishai/styles.css _site/styles.css
          test -s _site/index.html
          test -s _site/features.html
          test -s _site/security.html
          test -s _site/documentation.html
          test -s _site/download.html
          test -s _site/about.html
          test -s _site/styles.css
          test -s _site/logo.svg
      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v4
        with:
          path: _site
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4