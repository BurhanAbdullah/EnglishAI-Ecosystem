const AUTH_API='https://englishai-auth-api.onrender.com';
const MCP_API='https://englishai-mcp-gateway.onrender.com/mcp';
const MCP_PROTOCOL='2025-11-25';
function escapeHtml(value){return String(value).replace(/[&<>'"]/g,char=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[char]))}
async function getSession(){try{const r=await fetch(`${AUTH_API}/api/me`,{credentials:'include'});if(!r.ok)throw Error('auth');return await r.json()}catch{return{authenticated:false,unavailable:true}}}
async function mcpRequest(method,params={}){const r=await fetch(MCP_API,{method:'POST',headers:{'content-type':'application/json','accept':'application/json, text/event-stream'},body:JSON.stringify({jsonrpc:'2.0',id:crypto.randomUUID(),method,params})});if(!r.ok)throw Error(`MCP request failed: ${r.status}`);const text=await r.text();const data=text.match(/data:\s*(\{.*\})/s)?.[1]||text;const payload=JSON.parse(data);if(payload.error)throw Error(payload.error.message||'MCP error');return payload.result}
async function mcpCall(capability,tool,args={}){await mcpRequest('initialize',{protocolVersion:MCP_PROTOCOL,capabilities:{},clientInfo:{name:'modern-english-web',version:'1.1.0'}});return mcpRequest('tools/call',{name:'call_capability',arguments:{capability,tool,arguments:args}})}
async function mcpCapabilities(){await mcpRequest('initialize',{protocolVersion:MCP_PROTOCOL,capabilities:{},clientInfo:{name:'modern-english-web',version:'1.1.0'}});return mcpRequest('tools/call',{name:'list_capabilities',arguments:{}})}
function standardizeNavigation(session){const header=document.querySelector('.nav');if(!header)return;const nav=header.querySelector('.desktop-nav');if(!nav)return;const current=location.pathname.split('/').pop()||'index.html';const internal=current!=='index.html';document.body.classList.toggle('internal-page',internal);const links=[['Home','./index.html'],['Platform','./platform.html'],['Tutor','./learner.html'],['Capabilities','./capabilities.html'],['Architecture','./architecture.html'],['Research','./research.html'],['Docs','./docs.html'],['Community','./join.html']];nav.innerHTML=links.map(([label,href])=>`<a${href.endsWith(current)?' class="active"':''} href="${href}">${label}</a>`).join('');let actions=header.querySelector('.nav-actions');if(!actions){actions=document.createElement('div');actions.className='nav-actions';header.appendChild(actions)}if(session.authenticated){const name=session.user?.name||session.user?.login||'Account';actions.innerHTML=`<span class="nav-user">${escapeHtml(name)}</span><a class="nav-cta" href="./learner.html">My tutor →</a><button class="nav-logout" type="button" id="logout">Log out</button>`;document.getElementById('logout')?.addEventListener('click',async()=>{await fetch(`${AUTH_API}/api/logout`,{credentials:'include'});location.href='./index.html'})}else actions.innerHTML='<a class="nav-cta" href="./signup.html">Sign up / Log in →</a>'}
function injectAuthStyles(){if(document.getElementById('modern-english-auth-styles'))return;const s=document.createElement('style');s.id='modern-english-auth-styles';s.textContent='.nav-user{max-width:150px;overflow:hidden;text-overflow:ellipsis;white-space:nowrap;font-size:12px;font-weight:800;color:#555}.nav-logout{height:40px;padding:0 12px;border:1px solid #dedee2;border-radius:11px;background:#fff;color:#555;font:700 12px inherit;cursor:pointer}.nav-logout:hover{border-color:#e10600;color:#e10600}.auth-gate{padding:28px;border:1px solid #e8e8ec;border-radius:18px;background:#fff;text-align:center;box-shadow:0 20px 60px rgba(0,0,0,.06)}.auth-gate h2{font-size:32px}.auth-gate p{color:#666}.auth-gate .actions{justify-content:center}';document.head.appendChild(s)}
window.ModernEnglishAuth={AUTH_API,MCP_API,getSession,mcpRequest,mcpCall,mcpCapabilities};
document.addEventListener('DOMContentLoaded',async()=>{injectAuthStyles();const session=await getSession();standardizeNavigation(session);document.querySelectorAll('[data-auth-name]').forEach(el=>el.textContent=session.authenticated?(session.user?.name||session.user?.login||'Learner'):'Guest')});
